# Seasonal-adjustment predicate vocabulary

The accepted values of the EITS `seasonally_adj` predicate, as served by
the Bureau.

## Usage

``` r
SEASONALLY_ADJ
```

## Format

A named `list` of `scalar<character>` values: `yes`, `no`.
