% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CensusEconomicIndicators.R
\name{CensusEconomicIndicators}
\alias{CensusEconomicIndicators}
\title{CensusEconomicIndicators: Economic Indicators Time Series (EITS)}
\description{
Retrieves the US Census Bureau's Economic Indicators Time Series (EITS) — the
national economic prints such as advance retail sales (MARTS), business
formation (BFS), advance durable-goods orders (ADVM3), and new residential
construction (RESCONST). Every program is queried the same way and returns the
same tidy \link[=census_shapes]{EitsSeries} shape.
}
\details{
EITS is a predicate-driven grammar: a fixed variable list is always requested,
and the query is narrowed with predicates -- \code{time} (required), \code{category_code}
(industry/segment), \code{data_type_code} (sales vs inventory, level vs change), and
\code{seasonally_adj}. A \code{NULL} predicate returns every value of that dimension. The
result is long/tidy: one row per (period x category x data_type x adjustment).

The EITS API serves the national (\code{us}) geography only; \code{geo_for} defaults to
\code{"us"} and rarely needs changing. The wire keys \code{for}/\verb{in} are reserved words
in R, so the arguments are named \code{geo_for}/\code{geo_in} (only \code{geo_for} is used
here, as EITS has no nested geography).

Inherits from \link{CensusBase}; all methods honour the \code{async} flag set at
construction (sync returns a \code{data.table}, async a promise).
\subsection{Point-in-time caveat}{

The Census API returns only the \emph{latest revised} value for each period — it has
no vintage/as-of parameter. Retail sales, business formation, and construction
are heavily revised, so a naive historical pull is current-vintage only and is
a look-ahead trap for a backtest. Source revisable history from ALFRED and
snapshot forward pulls at fetch time; never feed the API's revised history into
a point-in-time model.
}
}
\examples{
\dontrun{
eits <- CensusEconomicIndicators$new()
# All categories/data-types of advance retail sales for 2024:
marts <- eits$get_series("marts", time = "2024")
# A single retail series, seasonally adjusted, over a multi-year range:
retail <- eits$get_series(
  "marts",
  category_code = "44000", data_type_code = "SM",
  seasonally_adj = "yes", time = "from 2019 to 2024"
)
# Convenience wrappers pin the program:
bfs <- eits$get_business_formation(time = "2024")

# Asynchronous:
eits_async <- CensusEconomicIndicators$new(async = TRUE)
main <- coro::async(function() {
  marts <- await(eits_async$get_series("marts", time = "2024"))
  print(marts)
})
main()
while (!later::loop_empty()) later::run_now()
}

}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[census:CensusBase]{census::CensusBase}} -> \code{CensusEconomicIndicators}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-CensusEconomicIndicators-get_series}{\code{CensusEconomicIndicators$get_series()}}
\item \href{#method-CensusEconomicIndicators-get_business_formation}{\code{CensusEconomicIndicators$get_business_formation()}}
\item \href{#method-CensusEconomicIndicators-get_retail_advance}{\code{CensusEconomicIndicators$get_retail_advance()}}
\item \href{#method-CensusEconomicIndicators-get_durable_goods_advance}{\code{CensusEconomicIndicators$get_durable_goods_advance()}}
\item \href{#method-CensusEconomicIndicators-get_housing_starts}{\code{CensusEconomicIndicators$get_housing_starts()}}
\item \href{#method-CensusEconomicIndicators-clone}{\code{CensusEconomicIndicators$clone()}}
}
}
\if{html}{\out{
<details open><summary>Inherited methods</summary>
<ul>
<li><span class="pkg-link" data-pkg="census" data-topic="CensusBase" data-id="initialize"><a href='../../census/html/CensusBase.html#method-CensusBase-initialize'><code>census::CensusBase$initialize()</code></a></span></li>
</ul>
</details>
}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-get_series"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-get_series}{}}}
\subsection{Method \code{get_series()}}{
Retrieve an EITS program's tidy series, filtered by the given
predicates. \code{time} is required (the API rejects a query without it);
accepts a year (\code{"2024"}, all its periods), a month (\code{"2024-03"}), or a
range (\code{"from 2020 to 2024"}). A \code{NULL} \code{category_code} /
\code{data_type_code} / \code{seasonally_adj} returns every value of that dimension.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$get_series(
  program,
  category_code = NULL,
  data_type_code = NULL,
  time = NULL,
  seasonally_adj = NULL,
  geo_for = "us"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{program}}{(scalar<character>) the EITS program code, one of
\code{names(EITS_PROGRAMS)}, e.g. \code{"marts"}.}

\item{\code{category_code}}{(scalar<character> | NULL) the industry/segment
predicate; \code{NULL} returns all categories.}

\item{\code{data_type_code}}{(scalar<character> | NULL) the item-type predicate;
\code{NULL} returns all data types.}

\item{\code{time}}{(scalar<character> | NULL) the required ISO time predicate: a
year, a month, or a \code{"from ... to ..."} range. \code{NULL} aborts with a
validation error naming the requirement.}

\item{\code{seasonally_adj}}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{\code{geo_for}}{(scalar<character>) the geography \code{for} clause (wire key
\code{for}); defaults to \code{"us"} (the only geography EITS serves).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(EitsSeries | promise<EitsSeries>) the tidy series, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-get_business_formation"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-get_business_formation}{}}}
\subsection{Method \code{get_business_formation()}}{
Business Formation Statistics (\code{bfs}): weekly and monthly
business-application counts, released ~5 days after week-end (weekly) and
~11-12 days after month-end (monthly); the monthly release revises the
prior two months. A convenience wrapper over \code{get_series("bfs", ...)}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$get_business_formation(
  time = NULL,
  category_code = NULL,
  data_type_code = NULL,
  seasonally_adj = NULL,
  geo_for = "us"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{time}}{(scalar<character> | NULL) the required time predicate (see
\code{get_series}).}

\item{\code{category_code}}{(scalar<character> | NULL) the industry predicate;
\code{NULL} returns all.}

\item{\code{data_type_code}}{(scalar<character> | NULL) the item-type predicate;
\code{NULL} returns all.}

\item{\code{seasonally_adj}}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{\code{geo_for}}{(scalar<character>) the geography \code{for} clause; default
\code{"us"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(EitsSeries | promise<EitsSeries>) the tidy series, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-get_retail_advance"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-get_retail_advance}{}}}
\subsection{Method \code{get_retail_advance()}}{
Advance Monthly Retail Trade (\code{marts}): the advance retail-sales
print, released ~9 working days after month-end -- the market-moving
estimate, subsequently revised. A convenience wrapper over
\code{get_series("marts", ...)}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$get_retail_advance(
  time = NULL,
  category_code = NULL,
  data_type_code = NULL,
  seasonally_adj = NULL,
  geo_for = "us"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{time}}{(scalar<character> | NULL) the required time predicate.}

\item{\code{category_code}}{(scalar<character> | NULL) the industry predicate;
\code{NULL} returns all.}

\item{\code{data_type_code}}{(scalar<character> | NULL) the item-type predicate;
\code{NULL} returns all.}

\item{\code{seasonally_adj}}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{\code{geo_for}}{(scalar<character>) the geography \code{for} clause; default
\code{"us"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(EitsSeries | promise<EitsSeries>) the tidy series, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-get_durable_goods_advance"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-get_durable_goods_advance}{}}}
\subsection{Method \code{get_durable_goods_advance()}}{
Advance Durable Goods orders (\code{advm3}, advance M3): durable-goods
new orders/shipments, released ~26 days after month-end -- a classic
leading macro indicator, revised by the full M3. A convenience wrapper over
\code{get_series("advm3", ...)}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$get_durable_goods_advance(
  time = NULL,
  category_code = NULL,
  data_type_code = NULL,
  seasonally_adj = NULL,
  geo_for = "us"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{time}}{(scalar<character> | NULL) the required time predicate.}

\item{\code{category_code}}{(scalar<character> | NULL) the industry predicate;
\code{NULL} returns all.}

\item{\code{data_type_code}}{(scalar<character> | NULL) the item-type predicate;
\code{NULL} returns all.}

\item{\code{seasonally_adj}}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{\code{geo_for}}{(scalar<character>) the geography \code{for} clause; default
\code{"us"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(EitsSeries | promise<EitsSeries>) the tidy series, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-get_housing_starts"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-get_housing_starts}{}}}
\subsection{Method \code{get_housing_starts()}}{
New Residential Construction (\code{resconst}): housing permits,
starts, and completions, released ~12-18 business days after month-end --
a classic leading indicator with large permit revisions. A convenience
wrapper over \code{get_series("resconst", ...)}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$get_housing_starts(
  time = NULL,
  category_code = NULL,
  data_type_code = NULL,
  seasonally_adj = NULL,
  geo_for = "us"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{time}}{(scalar<character> | NULL) the required time predicate.}

\item{\code{category_code}}{(scalar<character> | NULL) the segment predicate;
\code{NULL} returns all.}

\item{\code{data_type_code}}{(scalar<character> | NULL) the item-type predicate;
\code{NULL} returns all.}

\item{\code{seasonally_adj}}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{\code{geo_for}}{(scalar<character>) the geography \code{for} clause; default
\code{"us"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(EitsSeries | promise<EitsSeries>) the tidy series, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CensusEconomicIndicators-clone"></a>}}
\if{latex}{\out{\hypertarget{method-CensusEconomicIndicators-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CensusEconomicIndicators$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
