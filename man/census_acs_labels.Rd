% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{census_acs_labels}
\alias{census_acs_labels}
\title{Look up human-readable labels for ACS variables (keyless)}
\usage{
census_acs_labels(
  year,
  dataset = "acs1",
  variables = NULL,
  base_url = census_base_url(),
  async = FALSE
)
}
\arguments{
\item{year}{(scalar<count in [2005, Inf[>) the ACS vintage year.}

\item{dataset}{(scalar<character>) the ACS dataset, \code{"acs1"} or \code{"acs5"}.}

\item{variables}{(vector<character, 1..> | NULL) the variables to keep; \code{NULL}
returns the whole dictionary. Default \code{NULL}.}

\item{base_url}{(scalar<character>) the Census Data API base URL. Defaults to
\code{\link[=census_base_url]{census_base_url()}}.}

\item{async}{(scalar<logical>) if \code{TRUE}, returns a promise. Default \code{FALSE}.}
}
\value{
(VariableDictionary | promise<VariableDictionary>) one row per matched
variable, or a promise thereof.
}
\description{
Returns the labels and concepts for a set of ACS variables, so the wide
\link[=census_shapes]{AcsTable} whose columns are terse variable codes (\code{B19013_001E})
can be interpreted. A thin, cheap filter over the keyless variable dictionary.
}
\details{
Fetches the dataset's \code{variables.json} (the same source as \code{\link[=census_variables]{census_variables()}})
and returns only the rows for the requested \code{variables}; \code{NULL} returns the whole
dictionary. Use it to attach meaning to a \code{get_acs()} result — e.g. join its
\code{name} to your table's column names.
}
