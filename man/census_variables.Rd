% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{census_variables}
\alias{census_variables}
\title{List a dataset's variables (keyless)}
\usage{
census_variables(dataset_path, base_url = census_base_url(), async = FALSE)
}
\arguments{
\item{dataset_path}{(scalar<character>) the dataset path segment, e.g.
\code{"timeseries/eits/marts"} or \code{"2023/acs/acs1"}.}

\item{base_url}{(scalar<character>) the Census Data API base URL. Defaults to
\code{\link[=census_base_url]{census_base_url()}}.}

\item{async}{(scalar<logical>) if \code{TRUE}, returns a promise. Default \code{FALSE}.}
}
\value{
(VariableDictionary | promise<VariableDictionary>) one row per variable,
or a promise thereof.
}
\description{
Returns the variable dictionary for one dataset -- every queryable variable
with its label, concept, and whether it is required in a \verb{get=} list. No API
key is required.
}
\details{
Sourced from the dataset's \code{variables.json}. For an EITS program this is the
fixed measurement/predicate set (\code{cell_value}, \code{category_code}, ...); for an
ACS vintage it is tens of thousands of estimate/margin variables. Use it to
validate a variable name before a data call.
}
