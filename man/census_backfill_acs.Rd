% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{census_backfill_acs}
\alias{census_backfill_acs}
\title{Backfill an ACS variable set across survey years}
\usage{
census_backfill_acs(
  from,
  to,
  dataset = "acs1",
  variables,
  geo_for = "us",
  geo_in = NULL,
  api_key = census_api_key(),
  base_url = census_base_url(),
  sleep = 0
)
}
\arguments{
\item{from}{(scalar<count in [2005, Inf[>) the first survey year (inclusive).}

\item{to}{(scalar<count in [2005, Inf[>) the last survey year (inclusive).}

\item{dataset}{(scalar<character>) the ACS dataset, \code{"acs1"} or \code{"acs5"}.}

\item{variables}{(vector<character, 1..>) the \verb{get=} variables (max 50).}

\item{geo_for}{(scalar<character>) the geography \code{for} clause; default \code{"us"}.}

\item{geo_in}{(scalar<character> | NULL) the \verb{in} clause; default \code{NULL}.}

\item{api_key}{(scalar<character>) the Census API key. Defaults to the
\code{CENSUS_API_KEY} environment variable.}

\item{base_url}{(scalar<character>) the Census Data API base URL. Defaults to
\code{\link[=census_base_url]{census_base_url()}}.}

\item{sleep}{(scalar<numeric in [0, Inf[>) seconds to pause between years.
Default \code{0}.}
}
\value{
(data.table) the stacked multi-year AcsTable with a \code{year} column
(empty \code{data.table} with a \code{year} column if no year returned data).
}
\description{
Pulls the same ACS variables and geography across a range of survey years and
stacks them into one long table, adding a \code{year} column to distinguish the
vintages. A thin loop over \link{CensusACS}'s \code{get_acs()}, one request per year.
}
\details{
Each ACS year is a separate dataset (there is no \code{time} predicate), so this
iterates \code{from:to} and requests each year in turn. \strong{A year the Bureau did not
release} (e.g. standard \code{acs1} 2020) returns an HTTP error; that year is
\strong{skipped with a warning} rather than aborting the whole backfill. The
geography's \code{requires} chain is validated once up front (not per year). The
result is the dynamic-width \link[=census_shapes]{AcsTable} with a prepended \code{year}
(integer) column; column presence still follows the requested variables.
}
\examples{
\dontrun{
income <- census_backfill_acs(
  from = 2018, to = 2023, dataset = "acs1",
  variables = c("NAME", "B19013_001E"), geo_for = "state:*"
)
}

}
