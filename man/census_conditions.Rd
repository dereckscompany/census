% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{census_conditions}
\alias{census_conditions}
\title{Typed census conditions}
\description{
\code{census} raises \strong{classed conditions} so a caller branches on error \emph{type} and
reads structured \emph{fields} instead of matching the message text.
}
\details{
\subsection{Class taxonomy}{
\itemize{
\item \strong{Transport failures} nest specific -> general as
\verb{census_api_error_<status>} -> \code{census_api_error} ->
\verb{connectcore_api_error_<status>} -> \code{connectcore_api_error} ->
\code{connectcore_error}, carrying the fields \code{status}, \code{url}, \code{body_snippet}, and
\code{key_error}. Raised for a non-2xx HTTP status, a text/plain API error body, or
the missing/invalid-key HTML page.
\item \strong{Validation failures} nest \code{census_validation_error} -> \code{census_error} (the
domain root). Raised for a bad \code{program}, an out-of-range argument, or a
construction-time credential problem, before any request is made.
}

The missing/invalid-key case is special: because the API answers a keyless or
bad-key data query with an HTTP 302 redirect to \code{missing_key.html} /
\code{invalid_key.html}, and \code{httr2} follows the redirect, it arrives as an HTTP 200
with an HTML body. The envelope parser detects it (final URL, content type) and
synthesises a \code{census_api_error_401} carrying \code{key_error = TRUE}, so callers can
catch a 401 uniformly and read a message that names the activation requirement.

The \code{url} is stored with query-string credentials redacted (via
\code{\link[connectcore:scrub_url]{connectcore::scrub_url()}}, which lists \code{key} first in its sensitive-parameter
set), so logging \code{e$url} never leaks the key.
}
}
\seealso{
\link[connectcore:connectcore_conditions]{connectcore::connectcore_conditions}
}
