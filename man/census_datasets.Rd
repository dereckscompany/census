% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{census_datasets}
\alias{census_datasets}
\title{List the Census dataset catalogue (keyless)}
\usage{
census_datasets(catalogue_url = census_catalogue_url(), async = FALSE)
}
\arguments{
\item{catalogue_url}{(scalar<character>) the catalogue URL. Defaults to
\code{\link[=census_catalogue_url]{census_catalogue_url()}}.}

\item{async}{(scalar<logical>) if \code{TRUE}, returns a promise. Default \code{FALSE}.}
}
\value{
(DatasetCatalogue | promise<DatasetCatalogue>) one row per dataset, or a
promise thereof.
}
\description{
Returns every dataset the Census Data API publishes -- roughly 1,790 of them,
from the decennial count to the economic-indicator time series -- as a tidy
table. No API key is required.
}
\details{
Sourced from the machine-readable \code{data.json} catalogue at the host root. Use
\code{program_path} to address a dataset in the other functions (e.g.
\code{"timeseries/eits/marts"} for \code{\link[=census_variables]{census_variables()}}), and \code{is_timeseries} /
\code{is_microdata} to tell the shape families apart.
}
