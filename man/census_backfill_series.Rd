% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{census_backfill_series}
\alias{census_backfill_series}
\title{Backfill a multi-year EITS series}
\usage{
census_backfill_series(
  program,
  from,
  to,
  category_code = NULL,
  data_type_code = NULL,
  seasonally_adj = NULL,
  geo_for = "us",
  api_key = census_api_key(),
  base_url = census_base_url(),
  sleep = 0
)
}
\arguments{
\item{program}{(scalar<character>) the EITS program code, one of
\code{names(EITS_PROGRAMS)}, e.g. \code{"marts"}.}

\item{from}{(scalar<count in [1900, Inf[>) the first year to pull (inclusive).}

\item{to}{(scalar<count in [1900, Inf[>) the last year to pull (inclusive).}

\item{category_code}{(scalar<character> | NULL) the industry/segment predicate;
\code{NULL} returns all.}

\item{data_type_code}{(scalar<character> | NULL) the item-type predicate; \code{NULL}
returns all.}

\item{seasonally_adj}{(scalar<character in c("yes", "no")> | NULL) the
adjustment filter; \code{NULL} returns both.}

\item{geo_for}{(scalar<character>) the geography \code{for} clause; default \code{"us"}.}

\item{api_key}{(scalar<character>) the Census API key. Defaults to the
\code{CENSUS_API_KEY} environment variable.}

\item{base_url}{(scalar<character>) the Census Data API base URL. Defaults to
\code{\link[=census_base_url]{census_base_url()}}.}

\item{sleep}{(scalar<numeric in [0, Inf[>) seconds to pause between yearly
requests. Default \code{0}.}
}
\value{
(EitsSeries) the deduplicated, sorted multi-year series.
}
\description{
Pulls an EITS program's history across a range of years in one call, returning
a single tidy \link[=census_shapes]{EitsSeries} table. A thin loop over
\link{CensusEconomicIndicators}'s \code{get_series()}, one request per year, with the
results deduplicated and sorted.
}
\details{
The function constructs its own synchronous client from \code{api_key} / \code{base_url},
then requests \code{time = "<year>"} for each year in \verb{[from, to]}, sleeping \code{sleep}
seconds between requests. Rows are deduplicated by
\verb{(datetime, category_code, data_type_code, seasonally_adj)} and sorted. Narrow
the pull with \code{category_code} / \code{data_type_code} / \code{seasonally_adj} for a
specific series; leaving them \code{NULL} returns every combination, which for a wide
program over many years is a large table.

Point-in-time caveat: the Census API returns only latest-revised values, so a
backfill is current-vintage only -- a look-ahead trap for a backtest. See
\link{CensusEconomicIndicators} for the mitigation.
}
\examples{
\dontrun{
retail <- census_backfill_series(
  "marts",
  from = 2015, to = 2024,
  category_code = "44000", data_type_code = "SM", seasonally_adj = "yes"
)
}

}
