% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{census_geographies}
\alias{census_geographies}
\title{List a dataset's geography hierarchy (keyless)}
\usage{
census_geographies(dataset_path, base_url = census_base_url(), async = FALSE)
}
\arguments{
\item{dataset_path}{(scalar<character>) the dataset path segment, e.g.
\code{"timeseries/eits/marts"} or \code{"2023/acs/acs1"}.}

\item{base_url}{(scalar<character>) the Census Data API base URL. Defaults to
\code{\link[=census_base_url]{census_base_url()}}.}

\item{async}{(scalar<logical>) if \code{TRUE}, returns a promise. Default \code{FALSE}.}
}
\value{
(GeographyList | promise<GeographyList>) one row per geography level, or
a promise thereof.
}
\description{
Returns the geography levels a dataset serves, and for each nested level the
parent level(s) an \verb{in} clause must satisfy. No API key is required.
}
\details{
Sourced from the dataset's \code{geography.json}. The EITS programs return the single
national \code{us} level; ACS returns the full hierarchy (region, state, county,
place, ... down to block group), with \code{requires} naming the parents a query
must supply. Use it to validate a geography query against the hierarchy before a
data call.
}
